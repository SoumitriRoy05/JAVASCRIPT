// Create a JavaScript function that calculates the Body Mass Index (BMI) based on a person's weight (in kilograms) and height (in meters).

function bmiCalculator(weight,height)
{
    var bmi=weight/(height*height);
    return bmi;
}
